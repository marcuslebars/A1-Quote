[phases.setup]
nixPkgs = ["nodejs-22_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build:full"]

[start]
cmd = "node dist/index.js"

[variables]
NODE_ENV = "production"
